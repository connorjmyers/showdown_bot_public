import Showdown.Players.Player;
import Showdown.Showdown;
import org.junit.Assert;
import org.junit.Test;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

public class ShowdownTest {

    @Test
    public void setPlayersTest() {

        /*
        List<String> players = new ArrayList<>();
        Showdown showdown = new Showdown();
        Set<String> set;

        // If the showdown doesn't spawn with 100 people, something has gone wrong
        if (showdown.getPlayers().size() != 100) {
            Assert.fail();
        }

        for (Player player : showdown.getPlayers()) {
            players.add(player.getName());
        }

        set = new HashSet<>(players);

        // a set does not keep duplicates. If no duplicates, they will be the same size.
        if (set.size() != players.size()) {
            Assert.fail();
        }
         */
    }
}
